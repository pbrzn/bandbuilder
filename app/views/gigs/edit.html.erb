<% if @gig.errors.any? %>
  <%= render partial: 'errors', locals: {gig: @gig} %>
<% end %>

<h3>Edit <%= @gig.title %></h3>
<%= render partial: 'form', locals: {gig: @gig, url: music_director_gig_path(@music_director.id, @gig.id)}, submit_text: "Update Gig" %>
